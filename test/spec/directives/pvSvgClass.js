'use strict';

describe('Directive: pvSvgClass', function () {
  beforeEach(module('a3App'));

  var element;

  it('should make hidden element visible', inject(function ($rootScope, $compile) {
    // element = angular.element('<div pv-svg-class='{"className": "varName"}'></div>');
    // element = $compile(element)($rootScope);
    // expect(element.text()).toBe('this is the pvSvgClass directive');
  }));
});
