'use strict';

describe('Directive: pvScroll', function () {
  beforeEach(module('a3App'));

  var element;

  it('should make hidden element visible', inject(function ($rootScope, $compile) {
    // element = angular.element('<pv-scroll></pv-scroll>');
    // element = $compile(element)($rootScope);
    // expect(element.text()).toBe('this is the pvScroll directive');
  }));
});
